name: secret
on:
  push:
    tags:
      - "secret*"
jobs:
  label:
    strategy:
      matrix:
        os: [ubuntu-18.04] #, macos-latest, windows-latest
        java_version: [1.8] #, 10, 11, 12
    runs-on: ${{ matrix.os }}
    steps:
      - name: secret info
        run: |
          echo "${{ secrets.secret1 }}"
          
      
